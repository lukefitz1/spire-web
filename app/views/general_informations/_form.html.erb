<%= form_with(model: general_information, local: true) do |form| %>
  <% if general_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(general_information.errors.count, "error") %> prohibited this general_information from being saved:</h2>

      <ul>
      <% general_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :information_label %>
    <%= form.text_field :information_label, id: :general_information_information_label %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information, id: :general_information_information, cols: "75", rows: "20" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
