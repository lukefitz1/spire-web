<p id="notice"><%= notice %></p>

<% content_for :title, "View Artworks" %>

<h1>Artworks</h1>

<%= link_to 'New Artwork', new_artwork_path %><br /><br />

<h4>Search by Artist</h4>
<%= form_tag(artwork_search_path, method: :get) do %>
  <%= text_field_tag(:search) %>
  <%= submit_tag ("Search") %>  
<% end %>
<br /><br />

<%= form_tag('/artworks/destroy_multiple', method: 'delete') do %>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <!-- <th>Id</th> -->
      <th>Object ID</th>
      <th>Art Type</th>
      <th>Title</th>
      <th>Artist</th>
      <th>Date</th>
      <!-- <th>Medium</th> -->
      <!-- <th>Dimensions</th> -->
      <!-- <th>Frame Dimensions</th> -->
      <!-- <th>Current Location</th> -->
      <!-- <th>Source</th> -->
      <!-- <th>Date Acquired</th> -->
      <!-- <th>Notes</th> -->
      <!-- <th>Amount Paid</th> -->
      <!-- <th>Current Value</th> -->
      <!-- <th>Notes Image</th> -->
      <!-- <th>Notes Image Two</th> -->
      <!-- <th>Additional Info Image</th> -->
      <!-- <th>Additional Info Image Two</th> -->
      <th colspan="3"></th>
      <!-- <th data-sort-ignore="true"><input type="checkbox"></input></th> -->
      <th>Delete</th>
    </tr>
  </thead>

  <tbody>
    <% @artworks.each do |artwork| %>
      <tr>
        <td>
          <% if artwork.image.thumb.url %>
            <%= image_tag artwork.image.thumb.url %>
          <% end %>
        </td>
        <!-- <td><%= artwork.id %></td> -->
        <td><%= artwork.ojbId %></td>
        <td><%= artwork.artType %></td>
        <td><%= artwork.title %></td>

        <td>
          <% unless artwork.artist.blank? %>
            <%= artwork.artist.firstName %><%= ' ' %><%= artwork.artist.lastName %>
          <% end %> 
        </td>

        <td><%= artwork.date %></td>
        <!-- <td><%= artwork.medium %></td> -->
        <!-- <td><%= artwork.dimensions %></td>
        <td><%= artwork.frame_dimensions %></td>
        <td><%= artwork.currentLocation %></td>
        <td><%= artwork.source %></td>
        <td><%= artwork.dateAcquired %></td>
        <td><%= artwork.notes %></td>
        <td><%= artwork.amountPaid %></td>
        <td><%= artwork.currentValue %></td>

        <td>
          <% if artwork.notesImage.thumb.url %>
            <%= image_tag artwork.notesImage.thumb.url %>
          <% end %>
        </td>

        <td>
          <% if artwork.notesImageTwo.thumb.url %>
            <%= image_tag artwork.notesImageTwo.thumb.url %>
          <% end %>
        </td>

        <td>
          <% if artwork.additionalInfoImage.thumb.url %>
            <%= image_tag artwork.additionalInfoImage.thumb.url %>
          <% end %>
        </td>

        <td>
          <% if artwork.additionalInfoImageTwo.thumb.url %>
            <%= image_tag artwork.additionalInfoImageTwo.thumb.url %>
          <% end %>
        </td> -->

        <td><%= link_to 'Show', artwork %></td>
        <td><%= link_to 'Edit', edit_artwork_path(artwork) %></td>
        <td><%= link_to 'Destroy', artwork, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= check_box_tag "art_ids[]", artwork.id, false, class: 'selectable' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @artworks %><br /><br />

<%= submit_tag "Delete selected" %>
<% end %>